# 复制到本地 .env.local 或 .dev.vars（勿提交仓库）
# 生成：openssl rand -hex 32

LAB_SSO_SECRET=
LAB_DOJO_SERVER_KEY=

# VPS 就绪后再填，例如 https://lab.leolearnstotrade.com
# LAB_PUBLIC_BASE_URL=

# --- Spike runner（VPS 或可达 lab 的机器；本机无 URL 时会 fail closed）---
# LAB_SPIKE_LAB_BASE_URL=   # 可选，覆盖 LAB_PUBLIC_BASE_URL
# MAIN_APP_BASE_URL=        # Gate D SSO 联调

# --- 火山密钥（仅 VPS；勿写入 lab_config / git；runner 不读取、不上传）---
# API 路径 TBD，Spike 锁定后二选一：
# Path A 火山方舟 Ark（见 scripts/test/leo-001/）：
# ARK_API_KEY=
# Path B 视觉智能 visual（见 scripts/test/leo-003/、scripts/test/leo-004/）：
# VOLC_ACCESS_KEY=
# VOLC_SECRET_KEY=
